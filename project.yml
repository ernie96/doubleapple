name: DoubleTalk
options:
  bundleIdPrefix: com.doubletalkapple
  deploymentTarget:
    iOS: "16.0"
  createIntermediateGroups: true

settings:
  base:
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: "1"
    SWIFT_VERSION: "5.9"
    HEADER_SEARCH_PATHS:
      - "$(SRCROOT)/Sources/CDoubleTalk/include"
    CODE_SIGNING_ALLOWED: NO
    CODE_SIGNING_REQUIRED: NO

targets:
  DoubleTalkKit:
    type: framework
    platform: iOS
    sources:
      - Sources/CDoubleTalk
      - Sources/DoubleTalkKit
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.doubletalkapple.kit
        PRODUCT_NAME: DoubleTalkKit
        SWIFT_OBJC_BRIDGING_HEADER: Apps/DoubleTalkApp/DoubleTalkApp-Bridging-Header.h

  DoubleTalkVoiceExtension:
    type: app-extension
    platform: iOS
    sources:
      - Apps/DoubleTalkVoiceExtension
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.doubletalkapple.app.voiceextension
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: Apps/DoubleTalkVoiceExtension/Info.plist
        PRODUCT_NAME: DoubleTalkVoiceExtension
        TARGETED_DEVICE_FAMILY: "1,2"
        CODE_SIGN_ENTITLEMENTS: Apps/DoubleTalkVoiceExtension/DoubleTalkVoiceExtension.entitlements
        SWIFT_OBJC_BRIDGING_HEADER: Apps/DoubleTalkVoiceExtension/DoubleTalkVoiceExtension-Bridging-Header.h
    dependencies:
      - target: DoubleTalkKit

  DoubleTalkApp:
    type: application
    platform: iOS
    sources:
      - Apps/DoubleTalkApp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.doubletalkapple.app
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: Apps/DoubleTalkApp/Info.plist
        PRODUCT_NAME: DoubleTalk
        INFOPLIST_KEY_CFBundleDisplayName: DoubleTalk
        TARGETED_DEVICE_FAMILY: "1,2"
        CODE_SIGN_ENTITLEMENTS: Apps/DoubleTalkApp/DoubleTalkApp.entitlements
        SWIFT_OBJC_BRIDGING_HEADER: Apps/DoubleTalkApp/DoubleTalkApp-Bridging-Header.h
    dependencies:
      - target: DoubleTalkKit
      - target: DoubleTalkVoiceExtension
        embed: true
        codeSign: false
